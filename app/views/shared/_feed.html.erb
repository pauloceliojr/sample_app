<% if @feed_items.any? %>
    <ol class="microposts">
      <%= render partial: 'shared/feed_item', collection: @feed_items do %>
          # Nesse caso é necessária a chave 'partial:' porque é requisito para funcionamento da chave 'collection:'.
      <% end %>
    </ol>
    <%= will_paginate @feed_items, inner_window: 2 %>
<% end %>