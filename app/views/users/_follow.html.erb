<%= form_for(current_user.relationships.build(followed: @user), remote: true) do |f| %>
    <div><%= f.hidden_field :followed_id %></div>
    <%= f.submit 'Follow', class: 'btn btn-primary' %>
<% end %>